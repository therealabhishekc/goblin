# Application Environment
ENVIRONMENT=local
DEBUG=true
LOG_LEVEL=INFO

# Database Configuration (using PostgreSQL for local development)
# Note: No password in URL for security - use peer authentication or set PGPASSWORD env var
DATABASE_URL=postgresql://abskchsk@localhost:5432/whatsapp_business

# WhatsApp settings - MOVED TO AWS SECRETS MANAGER FOR SECURITY
# Use environment variables pointing to AWS Secrets Manager or local development values
WHATSAPP_TOKEN=your_local_dev_token_here
VERIFY_TOKEN=your_local_dev_verify_token_here  
PHONE_NUMBER_ID=your_local_dev_phone_number_id_here

# AWS settings (for local development, DynamoDB Local or disabled)
# DYNAMODB_TABLE will be set by CloudFormation in production
# For local dev, either comment out or use local DynamoDB instance
# DYNAMODB_TABLE=local-test-table
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=dummy
AWS_SECRET_ACCESS_KEY=dummy

